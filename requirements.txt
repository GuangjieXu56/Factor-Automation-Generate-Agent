# FactorGenAgent runtime dependencies
# Install with: pip install -r requirements.txt
#
# Notes:
# - This list is aligned with the current Python source under agents/, utils/,
#   main.py, and run.py.
# - The local embedding model files under hub/ are not installed by pip.
# - If you want GPU Faiss, replace faiss-cpu with a compatible GPU build.

openai>=1.40.0
pandas>=2.0.0
numpy>=1.24.0
pdfplumber>=0.10.0
faiss-cpu>=1.7.4
langchain-text-splitters>=0.2.0
tiktoken>=0.7.0
sentence-transformers>=2.6.0
torch>=2.1.0
pyarrow>=14.0.0
